{
  "name": "balance-bites-ops-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix hub",
    "build": "npm run build --prefix hub",
    "start": "npm run start --prefix hub",
    "lint": "npm run lint --prefix hub"
  }
}
